# 更新日志

本文件记录 v3.0 分支的开发更新，按**大功能**归类（不再按零散日期逐条记）。

---

## v3.0（2026-07）

### 1. 灵动岛

屏幕边缘常驻药丸浮窗，全面重写：

- 逐像素 alpha 渲染（`UpdateLayeredWindow` + Pillow 超采样），无毛边抗锯齿
- 60fps 临界阻尼弹簧动画，状态切换交叉淡入，DPI 自适应
- 拖拽吸附任意屏幕边缘，位置持久化；本地人声检测，开口即切「正在听」
- 实时冒字打字机光标、音量脉动红点、识别完成绿勾仪式帧
- 配置项：自动隐藏 / 送达流光 / 实时冒字，均可独立开关
- **修复**：每次按键 `arm` 时重新置顶（Tk `-topmost` + Win32 `SetWindowPos`），不被其他窗口遮挡

### 2. 送达流光

识别完成后从灵动岛向鼠标位置发射流光弹幕：

- 7 套内置风格（彗星 / 流星 / 樱粉 / 霓虹 / 极简 / 连续能量弹 / 终极闪光）
- 按字数连射，压轴大弹正中靶心；支持随机换色、彩虹弹幕
- 总时长自适应（1~2 秒），色彩 / 拖尾 / 粒子等可在 `config_client.py` 完全自定义

### 3. 识别管线

- **增量预览**：录音时每 ~0.6s 只读识别未消费缓冲，首字延迟从 ~3s 降至 ~0.6s
- **two-pass 终段重识别**：松开键后整段全量重识别替换分段拼接，消除接缝；超长录音自动回退分段模式
- 录音流式分段实时冒字、录音时可选静音系统声音

### 4. UV 双包与工程化

- `capswriter-client` / `capswriter-server` 独立安装（Git 子目录 / PyPI），不含模型权重
- 移除 PyInstaller 集成 exe 构建链（`build.spec`、`zip_release.py` 等）
- 源码开发：根目录 `uv sync` + `uv run start_server.py` / `start_client.py`
- **修复**：根 `pyproject.toml` 通过 workspace 引用双包，同步后不再缺 `websockets` 等运行时依赖
- **修复**：ctypes 调用、状态机时序、底边停靠可见性等若干小问题

### 5. 模型获取

发行包不含模型。首次运行服务端会提示缺失文件与下载页，解压至工作目录 `models/` 即可。
